Archived files
==============

This folder preserves older site versions, unused legacy assets, and PDF-source
HTML files that are no longer part of the live portfolio site root.

- `legacy-site/`: previous homepage iterations and unused legacy images.
- `pdf-source-html/`: HTML files used to generate the portfolio PDFs.
- `system-files/`: local system artifacts preserved instead of deleted.
